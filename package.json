{
  "name": "draft-app-backend",
  "version": "1.0.0",
  "description": "bäkkäri draft-appiin",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../draft-app/ && npm run build && cp -r dist ../notes-backend",
    "deploy": "flyctl deploy",
    "deploy:full": "npm run build:ui && npm run deploy", 
    "logs:prod": "fly logs" 
  },
  "author": "Kalle Hoikkala",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
